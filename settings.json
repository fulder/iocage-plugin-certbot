{
  "serviceget": "/usr/local/bin/certbotget",
  "serviceset": "/usr/local/bin/certbotset",
  "options": {
    "cloudflare_api_token": {
      "type": "string",
      "name": "Cloudflare api token",
      "description": "Cloudflare token to use for dns challenge",
      "default": "",
      "requirerestart": false
    },
    "cloudflare_domains": {
      "type": "string",
      "name": "Cloudflare domains",
      "description": "One or multiple domains (comma separated) to update using Cloudflare dns challenge",
      "default": "",
      "requirerestart": false
    },
    "cron": {
      "type": "string",
      "name": "Certs update schedule",
      "description": "Cron expression for when to run the update certs script. Default: every second month",
      "default": "0 0 * */2 *",
      "requirerestart": false
    }
  }
}